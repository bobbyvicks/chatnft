node_modules/
.DS_Store
*.log
.vercel
.probe/

# Playwright output
test-results/
playwright-report/

# Throwaway browser probes. These are written and deleted constantly while
# measuring something; they are not tests and must never be committed.
probe.spec.js
probe.config.js

# The scheduled run's lock. Machine-local, never travels, never committed.
.runlock

# Agent-written probe specs. Verifier subagents write throwaway specs while
# checking a claim; they are not tests and must never join the suite.
tests/zz*.spec.js
pw.*.config.js
